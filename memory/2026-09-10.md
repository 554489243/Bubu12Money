# 2026-09-10 开发日志

## 项目概况
个人记账应用（Vue3 + Vant4 + PWA + IndexedDB），工作目录 D:\myData\demo\Bookkeeping，开发服务器 http://localhost:5182

## 今日完成

### Bug 修复
- 删除父分类时同时删除子分类（避免孤儿分类）
- 统计页切换账本自动刷新图表（watch currentBookId）
- 分类管理默认金额输入非数字时崩溃（isNaN 校验）
- 清除残留 console.log（bookStore、Stats）
- Profile「云端同步」「关于」添加 showToast 点击提示
- BookSelector.vue @keyframes 缺少闭合括号修复

### 计算器安全改进
- 表达式求值从 Function() 改为简单数学解析（支持 + - * / 优先级）

### 图例系统全面优化（用户要求：重新核对图例，优化图案、样式、类型与名称对应）

#### 1. 创建色彩系统 (utils/colors.ts)
- 为所有父分类和子分类定义固定颜色 (bg/text/light)
- 支出用暖色系（橙/蓝/粉/棕/紫/红/青/灰），收入用绿色系
- 导出 getCategoryColor(name) 函数，自动匹配子→父→默认

#### 2. 修复分类图标冲突 (categories.ts)
- 车贷 🚗→🚙（与交通父分类区分）
- 游戏 🎮→🕹️（与娱乐父分类区分）
- 培训 📚→📝（与教育父分类区分）
- 保险 🛡️→🩺（与车险区分）
- 杂项 📦→🍽️/🗂️/🔧（各父分类下不同）

#### 3. 统计页 (Home.vue)
- 饼图使用分类固定颜色（而非按索引分配）
- 图例名称添加分类色彩背景
- 收入分类使用绿色系

#### 4. 明细页 (List.vue)
- 记录图标背景使用分类 light 色
- 父分类标签使用分类色彩背景

#### 5. 记账页 (Edit.vue)
- 父分类 chip 选中状态使用分类色彩
- 子分类图标框使用分类色彩（未选中浅色背景，选中深色背景+白色图标）

#### 6. 分类管理页 (Categories.vue)
- 父分类左侧添加分类色彩竖线
- 父分类名称使用分类文字颜色
- 子分类图标添加分类浅色背景框

#### 7. 账本选择器 (BookSelector)
- 账本图标添加账本色彩背景

#### 8. 全局色彩统一
- 明细作为主页（原 List 页 → /）
- 统计合并到主页（原 Home 页 → /stats）
- TabBar: 主页/统计/我的
- **星期显示**: dayjs 设置 zh-cn locale，主页和明细统一显示星期（周一、周二...）
- **卡片圆角**: 统一使用 CSS 变量 --radius(12px) / --radius-lg(16px)
  - 统计卡从 radius-lg 改为 radius
  - 分类 parent-group 从 14px 改为 radius
  - Edit: cat-icon-box, cat-parent-chip, popup-cat-icon-box, salary-section, salary 按钮从 10-12px 改为 radius
  - Books: book-icon-wrap, icon-option, form-input 从 10px 改为 radius
  - Categories: form-input, icon-option 从 10px 改为 radius
- **空状态内边距**: 统一为 padding: 60px 20px
- **区块标题**: 统一为 padding: 0 2px, font-size: 14px
- **Books 字体**: book-name 从 11px 改为 14px，book-stats 从 11px 改为 12px
- **BookSelector 下拉框**: 统一 max-height: 255px + 动画 + 滚动条样式
- **明细页日期**: 从 formatDate 改为 formatDateWeekday（显示星期）

### UI 美观优化
- **全局 CSS**: 统一 CSS 变量（--shadow, --shadow-sm, --radius, --radius-lg），大屏幕 box-shadow，page-content -webkit-overflow-scrolling:touch
- **Home**: 卡片点击 scale(0.98) 反馈，概览卡 box-shadow
- **List**: 筛选栏 sticky 吸顶，卡片点击反馈
- **Stats**: 饼图空状态提示
- **Edit**: 金额区/键盘按钮使用 CSS 变量圆角
- **Profile**: 菜单项点击高亮反馈
- **TabBar**: backdrop-filter blur(10px) 毛玻璃效果，按钮点击反馈

## 待测试
- 工资单功能端到端
- 二级筛选准确性
- 记录卡父分类显示
- 分类管理 CRUD
- 自动填充/清除金额行为

## 后续计划
- 安装 Git（当前网络慢，暂缓）
- 部署到 Vercel 或 GitHub Pages
- 明细页搜索功能
